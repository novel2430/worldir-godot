WorldIR Godot V1-A/V1-B / ArtLab Policy 开发备忘
日期：2026-08-15（Asia/Shanghai）

一、版本与定位

- 当前集成分支：codex/to-v1-b-artlab-policy
- 原始 V1-A 开发基线：1372c875397ff1f829daa50277182caf6f9b05cf
- V1-B 主线基准：to-V1-B @ 575b7bceccce8c5b660d235f877c86d39098f7a4
- 原始 V1-A ArtLab 提交：a22d64c
- 当前 V1-B ArtLab 移植提交：b61b816
- 当前集成成果发布目标为远端 to-V1-B；最终发布提交以该远端分支最新提交为准。
- ArtLab 是参数化生成实验沙盒，不是 V1-A 主项目的一部分。
- 本轮工作的目标是把 ArtLab 已验证的生成经验沉淀为 V1-A/V1-B 集成项目 Backend
  自有的 Realization Policy，帮助现有合法 World IR 更好地生成和描述世界，而不是
  合并两个项目。移植以 to-V1-B 为准，不倒灌或覆盖 V1-B 的 revision/rebase/
  transition 实现。

二、不可破坏的边界

1. World IR、Compiler Server、Runtime Facts 和现有四根对象结构不变。
2. WorldBackend、ChunkGenerator、ChunkManager、SceneRuntime 的公开方法签名不变。
3. Policy 不写回 World IR，不成为新的外部输入，也不改变 Resolved 输出协议。
4. 优先级固定为：

   显式 World IR 语义
     > 已解析的 placement / prototype occupancy 约束
     > Backend 自有 ArtLab Realization Policy

5. Policy 不得改变显式 Distribution 的数量、排列、placement relation 或稳定实例
   identity；当前人口参数只作用于 Backend 自有的森林 dressing。
6. Same Inputs 仍须确定性输出，且不得依赖 Chunk 生成顺序或可变全局状态。

三、已完成工作

[A] Policy 数据与加载

- 增加 data/configs/artlab_realization_policy.json，集中管理 terrain、surface、
  dressing 参数。
- 增加 scripts/backend/realization_policy.gd，负责读取、校验、默认值回退和警告。
- 内置兼容默认值与检入配置一致；配置缺失或字段非法时保留既有可运行行为。
- 静态验证器已检查 Policy JSON、WorldIR 引用、res:// 路径和 TSCN 资源引用。

[B] Terrain 与 Surface

- 地形宏观/细节频率、幅度和高度限制改为 Policy 驱动。
- 森林起伏、聚落平整、Region 内外过渡宽度改为 Policy 驱动。
- 海岸沙滩、湿沙、水下坡度和陆地衔接参数改为 Policy 驱动。
- 道路肩部、道路地形混合、建筑泥土地表和建筑平整影响改为 Policy 驱动。
- meadow、forest、soil、sand、wet sand、road dirt 调色板与材质参数已接入。
- Policy 依据现有合法 Region 类型在空间上解析，不把整个 160m Chunk 强行归为
  单一 ArtLab profile。

[C] Population 与 Forest Dressing

- 森林 dressing 使用以下参数化语义：

  candidate area
    x acceptance probability
    x edge profile
    x building clearing influence
    x road/path corridor influence
    x shared occupancy
    -> realized decoration instances

- dead_tree、rock、bush、grass 分层配置了候选面积、接受概率、cap、cluster、
  edge mode 和道路净距。
- 建筑 clearing 半径会参考实际 TSCN 可视 footprint。
- 道路和路径保留硬碰撞/净距核心，同时增加平滑外部走廊，避免植被出现笔直截断。
- 显式 Entity 和 Distribution 仍先于 Backend dressing 占用空间。

[D] Runtime 表现

- SceneRuntime 已消费新 terrain/surface/dressing 结果进行场景实例化和显示。
- 没有增加新的外部运行时协议字段或调用入口。

[E] Chunk-local 依赖闭包修复

- ChunkGenerator 在进入 Backend 前对被方向/ownership 规则裁掉的对象执行递归依赖
  闭包裁剪。
- 已覆盖 placement relations、Network topology 和 Distribution density selector。
- 例：西北 Chunk 裁掉 east-anchored coast 后，同时裁掉依赖 coast 的 town、依赖
  town 的 road 和 houses，独立 forest/forest_trees 继续正常生成。
- 原始 IR 不被修改；原始 IR 中未知或非法 target 仍走既有错误路径。
- 没有放宽 Region arbitration，没有修改 PlacementSolver 的缺失目标行为，也没有
  吞掉 Backend/Contract 错误。

[F] 文档、测试与证据

- 增加 docs/artlab_realization_policy.md，并更新开发规范、Backend contract 说明和
  README。
- 增加 Policy 单元/边界测试、forest dressing 回归和 coastal town 依赖闭包回归。
- Windows 真实引擎：Godot 4.7.1 stable，RTX 5070，OpenGL Compatibility。
- 原始 V1-A ArtLab 完整测试结果：21/21 通过。
- 移植到 to-V1-B 后的完整测试结果：30/30 通过，包含 V1-B revision kernel、
  current-chunk transaction、boundary blend、Preview scheduler、transition modes 和
  Step 5 end-to-end。
- Chunk 依赖闭包增强用例单独复测通过。
- 真实 3x3 活动窗口生成成功：9 个 active chunks。
- 当次统计：1 个 entity、802 个显式 distribution instances、669 个 decorations；
  对照基线 decorations 为 660。
- 已保存六个 ArtLab Policy 机位和六个 1372 基线同机位截图：
  screenshots/artlab_policy/
  screenshots/baseline_1372/
- Windows C:\worldir-godot 原项目未被修改；测试使用隔离临时副本并已清理。

四、当前结论

- ArtLab 经验已经以 Backend-owned、可配置、确定性的方式进入以 to-V1-B 为准的
  V1-A/V1-B 集成项目。
- 当前接入改善的是“同一合法 World IR 如何被实现”，不是扩展“World IR 能表达什么”。
- 已落地部分不要求修改任何对外输入、输出、接口或协议。
- 已修复旧策略在 Chunk-local interpretation 中制造悬空语义依赖的问题；ArtLab
  Policy 的 3x3 真实生成路径也通过了该回归。
- 当前画面差异是增量优化，不应把现有截图解读为最终美术质量基准。

五、后续 TODO：不修改对外契约即可继续

状态说明：本轮按“功能优先”发布。`[x]` 表示实现且已完成相应快速验证；`[~]` 表示
实现/用例已进入仓库，但真实引擎扩展运行仍待补；`[ ]` 表示留待后续。性能优化和
扩展稳定性不作为本次发布阻塞项。

P0 — 回归与可维护性

[x] 为依赖闭包增加更小的专用测试文件，覆盖多级链、多个 target、topology via、
    density gradient selector 和互相引用，避免只依赖 coastal-town 综合用例。
[x] 增加“生成顺序不同但 Chunk 结果一致”的 Policy 确定性回归。
[x] 增加 Policy 缺失、JSON 损坏、字段类型错误、局部字段缺失的 fallback 测试。
[x] 明确 Policy format 版本升级规则和未知版本处理策略，并增加 Policy fingerprint。
[~] 已增加 tools/run_godot_tests.ps1，可枚举测试并输出 JSON 汇总；完整脚本运行待补。

P1 — 视觉标定

[~] 已增加 inland village、southern coast 两套合法 World IR fixture，以及五场景
    矩阵测试/截图工具；完整矩阵运行和人工验收待补。
[ ] 用更多合法 World IR 场景完成 forest、settlement、coast、road 的组合标定，不只
    使用当前 coastal-town 样例。
[ ] 调整森林边缘 profile，使近边界层次更明显，同时避免草/灌木在道路旁形成硬线。
[ ] 继续标定建筑 clearing：分别覆盖小屋、长屋和大 footprint 建筑。
[ ] 标定 coast 的沙滩宽度、湿沙过渡和水下坡度，覆盖不同 coast mask 形状。
[ ] 为每次参数调整保留同 seed、同相机、同 IR 的 before/after 截图和实例统计。
[x] 已建立 docs/artlab_visual_acceptance.md 轻量视觉验收清单：Chunk seam、道路
    净距、建筑入口、海岸水线、重复图案、
    漂浮/穿插对象。

P1 — 性能与规模

本节按 2026-08-15 的功能优先决策整体后移；仓库中仅保留测试/采样工具骨架，不把
未运行的数据写成性能结论。

[ ] 在 Windows 真机记录初始 3x3、单 Chunk revision 和 streaming 移动时的 CPU
    时间、帧耗时、实例数量与峰值内存。
[ ] 检查高密度 dressing 下 shared occupancy 查询成本，并在不改变 identity 的前提
    下优化热点。
[~] 已增加远距离 streaming 路径、历史 Chunk restore 和 seam 的扩展用例，真实引擎
    完整运行待补。

P2 — Policy 工具化（仍保持内部）

[~] 已增加只读 Policy dump/诊断输出，说明每个 Chunk/位置最终命中的 Region role、
    layer 和 influence，便于调参排查。
[x] 增加配置 JSON Schema/字段说明，注明单位、合法范围、默认值和影响范围。
[x] 已评估开发环境 Policy 热重载：当前不实现。生成固定绑定加载时的 Policy
    fingerprint，避免 Same Inputs 随磁盘状态漂移。
[x] 已将 ArtLab 实验记录整理为参数变更日志，区分“观察”“假设”“已验证规律”。

五（附）、本次功能优先发布的验证状态（2026-08-15）

已通过（Windows Godot 4.7.1 真实引擎）：

- tests/test_realization_policy.gd
- tests/test_chunk_dependency_closure.gd
- tests/test_artlab_policy_determinism.gd

已通过（本机快速校验）：

- python3 tools/validate_project.py
- git diff --check
- Policy/config/fixture JSON 解析

按用户要求不阻塞发布、明确留待后续：

- tests/test_artlab_scenario_matrix.gd（25 次完整 Chunk 生成，运行中未见脚本错误，
  因发布时间优先主动停止，不能记作通过）
- tests/test_artlab_streaming_scale.gd
- tests/test_artlab_visual_calibration.gd
- tools/run_godot_tests.ps1 的完整套件报告
- tools/capture_artlab_matrix.gd 的五场景图形渲染与人工清单
- tools/benchmark_artlab_policy.gd、CPU/帧耗时/峰值内存和高密度热点优化

上述待测项不涉及新增对外契约；下一节所列契约/架构候选仍全部未实现。

六、后续候选：需要先批准契约/架构变化，不得直接实现

[ ] 在 World IR 中表达 snow_forest、research_base 或其他新的 profile/biome ID。
[ ] 增加 biome、weather、lighting、atmosphere、precipitation 等 Compiler 字段。
[ ] 扩展 ResolvedTerrain 的 surface channels。
[ ] 增加跨 Chunk persistent-structure influence payload，使未拥有结构的邻接 Chunk
    也能稳定收到其地形/植被影响。
[ ] 增加 Resolved environment 输出，并由世界级 lighting/environment coordinator
    消费。
[ ] 将 ArtLab 的固定五 Chunk 布局、mutation key、HUD 或证据采集生命周期接入
    主项目运行时。

上述每一项都必须先分别说明：为何现有契约无法表达、拟修改的输入/输出字段、版本
兼容策略、确定性影响、旧客户端行为、迁移与回滚方案。未经明确批准不应实施。

七、建议的下一阶段验收门槛

1. 现有全部测试继续通过，新增 Policy/闭包测试通过。
2. 同 seed + 同 IR + 同 Policy version 的结果可重复，且与生成顺序无关。
3. 3x3 初始化、streaming 移动和单 Chunk revision 均无新增错误。
4. 不新增 World IR 根字段，不改变任何现有公开方法签名或序列化协议。
5. 视觉对照至少覆盖 forest、coast、settlement、road、building clearing 和 Chunk seam。
6. 性能数据达到项目既有 streaming 预算；若无正式预算，先记录基线而不擅自设定。
7. 所有需要契约变化的候选仍保持未实现，直到获得单独确认。

八、常用入口

- Policy 配置：data/configs/artlab_realization_policy.json
- Policy 说明：docs/artlab_realization_policy.md
- 移植/契约变更交接：docs/artlab_policy_migration_contract_handoff.md
- Policy 加载：scripts/backend/realization_policy.gd
- Terrain 实现：scripts/backend/terrain_resolver.gd
- Dressing 实现：scripts/backend/forest_dresser.gd
- Chunk 依赖闭包：scripts/chunk/chunk_generator.gd
- Runtime 呈现：scripts/runtime/scene_runtime.gd
- Policy 测试：tests/test_realization_policy.gd
- Coastal-town 回归：tests/test_coastal_town_houses.gd
- 截图工具：tools/capture_artlab_policy.gd
- 静态验证：tools/validate_project.py

九、恢复点

- 如需回看原始 V1-A 接入前状态：1372c875397ff1f829daa50277182caf6f9b05cf
- 如需回看未接入 ArtLab 的最新 V1-B 状态：575b7bceccce8c5b660d235f877c86d39098f7a4
- 如需回看原始 V1-A ArtLab 实现：a22d64c
- 如需回看移植到 V1-B 后的 ArtLab 实现：b61b816
- 不建议用破坏性 reset；优先新建分支、使用 git show/diff 或 revert。
